{
  "scripts": {
    "help": "php bin/console",
    "start_server": "php bin/console server:run",
    "start_watch": "yarn run encore dev --watch",
    "check_routes": "php bin/console debug:router",
    "new_controller": "php bin/console make:controller",
    "new_entity": "php bin/console make:entity",
    "new_fixture": "php bin/console make:fix",
    "migrate_diff": "php bin/console doctrine:migrations:diff",
    "migrate": "php bin/console doctrine:migrations:migrate",
    "load_fixtures": "php bin/console doctrine:fixtures:load"
  },
  "devDependencies": {
    "@symfony/webpack-encore": "^0.22.2",
    "node-sass": "^4.9.2",
    "sass-loader": "^7.0.3",
    "webpack-notifier": "^1.6.0"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "gsap": "^2.0.1"
  }
}
